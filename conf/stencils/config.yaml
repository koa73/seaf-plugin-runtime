schemas:
  seaf.company.ta.services.dc_regions:
    layer: "Регион"
    data_lock:
      - OID
      - schema
    # fields:                  # Phase 2 (рендер пока fallback'ом на text/textarea).
    #   stand:
    #     widget: combo         # text | textarea | combo | radio | checkbox
    #     options: [PROD, DEV, TEST]
    #     required: true
  seaf.company.ta.services.dc_azs:
    layer: "Зона доступности"
    parent:
      schema:
        - seaf.company.ta.services.dc_regions
      field: region
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.dc_offices:
    layer: "Офис"
    mirror: "Сегменты офиса"
    sync_title_with_label: true
    parent:
      schema:
        - seaf.company.ta.services.dc_regions
      field: region
    data_lock:
      - OID
      - schema
    data_hidden:
      - link
  seaf.company.ta.services.dcs:
    layer: "ЦОД"
    mirror: "Сегменты ЦОД"
    sync_title_with_label: true
    parent:
      schema:
        - seaf.company.ta.services.dc_azs
      field: availabilityzone
    data_lock:
      - OID
      - schema
    data_hidden:
      - link
  seaf.company.ta.services.network_segments:
    layer: "Зоны безопасности"
    parent:
      schema:
        - seaf.company.ta.services.dcs
        - seaf.company.ta.services.dc_offices
      field: location
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.networks:
    layer: "Услуги связи"
    data_lock:
      - OID
      - schema
  seaf.company.ta.components.networks:
    layer: "Сетевое оборудование"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.kbs:
    layer: "Сервисы КБ"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.clusters:
    layer: "Сервисы ТА кластер"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.compute_services:
    layer: "Сервисы ТА"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.monitorings:
    layer: "Мониторинг"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.backups:
    layer: "BackUp"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.cluster_virtualizations:
    layer: "VM"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.services.k8s:
    layer: "K8S"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
  seaf.company.ta.components.user_devices:
    layer: "User Devices"
    parent:
      schema:
        - seaf.company.ta.services.network_segments
      field: segment
    data_lock:
      - OID
      - schema
