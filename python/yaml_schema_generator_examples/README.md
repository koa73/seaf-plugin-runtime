# yaml_schema_generator examples (dev only)

Копии эталонных YAML из проекта `yaml_schema_generator_test` для ручной сверки Export.

**Не входят** в `seaf-plugin-runtime.tar.gz` (см. `release/runtime/build-runtime.sh`).

Источник: `/home/oleg/PycharmProjects/yaml_schema_generator_test/yaml_schema_generator/examples/`.
